source "https://rubygems.org"

# GitHub Pages compila el sitio con esta gema; fija las versiones de Jekyll y
# de los plugins permitidos. Para previsualizar en local:
#   bundle install
#   bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # necesario con Ruby >= 3.0
